services:
  - type: web
    name: dart-mcp-server
    env: python
    buildCommand: echo "No build needed - dependencies installed at runtime"
    startCommand: python3 main.py
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
